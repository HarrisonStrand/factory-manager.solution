@{
  Layout = "_Layout";
}

@using Factory.Models;

<h1>Hired Engineers:</h1>

@if (@Model.Count == 0)
{
  <h2>No Engineers have been hired yet...</h2>
}

@foreach(EngineerMachine element in Model)
{
  <li>@Html.ActionLink($"{element.EngineerName}", "Details", new { id = element.EngineerId })</li>
  @* <li>@Html.ActionLink($"{element.MachineName}", "Details", new { id = element.MachineId })</li> *@
}
@* @foreach(Machine machine in Model)
{
  <li>@Html.ActionLink($"{machine.MachineName}", "Details", new { id = machine.MachineId })</li>
} *@

<p>@Html.ActionLink("Add New Engineer", "Create", "Engineers")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>